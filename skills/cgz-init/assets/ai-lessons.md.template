# AI 教训日志

每次 AI 出错 / 跑偏 / 返工后，按本文件格式追加一条。最新的写在最上面。

新会话启动时，agent 必须先读这个文件再回答。

---

## How to log

每条按以下 6 字段记录：

```markdown
## YYYY-MM-DD · [一句话主题]

**Trigger** —— 触发场景：用户当时在问什么、做什么。

**Wrong** —— 错误行为：AI 实际给出的回复或动作。

**Correction** —— 用户纠正：用户的反驳原话。

**Rule** —— 提炼规则：一句话写出未来该怎么做。

**Where** —— 生效位置：AGENTS.md / 本文件 / specific skill。

**Next read** —— 下次启动时必须先读什么。
```

字段说明：

- `YYYY-MM-DD` 用绝对日期，禁止「上周」「昨天」。
- `Rule` 必须是命令式短句（必须 / 禁止 / 立刻），禁止「应该 / 尽量」。
- `Where` 必须落到具体文件路径，禁止「以后注意」类无落地点。

---

## 教训记录

<!-- 在此追加新教训。最新的写在最上面，旧的往下沉。 -->

<!-- 示例（删掉这块再开始记）：

## 2026-01-15 · 不要在用户问「这个按钮为什么藏起来」时顺便给重构方案

**Trigger** —— 用户问 UI 设计原因，期望一个解释。

**Wrong** —— 给了 4 条原因 + 「要不要顺便在另一个页面加说明？10 分钟」。

**Correction** —— 用户：「我没问你重构，跑偏了」。

**Rule** —— 用户问「为什么 X」时只答原因；禁止顺手给改动方案，除非用户问。

**Where** —— `skills/focused-discussion/references/discussion-discipline.md` 的「反模式 · 自我新增议题」。

**Next read** —— 新会话先读 focused-discussion 的 references。

-->
